{
  "id": "single-call",
  "title": "What is the structure of the Astro wiki?",
  "question": "List the documentation topics for withastro/astro.",
  "servers": {
    "deepwiki": ["read_wiki_structure"]
  },
  "why": "One call, small answer. Code mode has nothing to optimise and still pays for the program. This task exists to be lost.",
  "expectCodeModeWins": false,
  "timeoutMs": 60000,
  "expect": {
    "mustMention": ["Deployment Adapters", "Integration System"],
    "note": "Deterministic substring grading. A model grading a model would put the thing under test inside the measurement. These strings were chosen from what the servers actually returned on 2026-09-21, so a server change makes the grade fail loudly rather than drift quietly."
  }
}
