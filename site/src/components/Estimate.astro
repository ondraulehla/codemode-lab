---
/**
 * The marker that must sit next to every token figure on this page.
 *
 * The rule the project holds itself to: bytes are counted, tokens are estimated,
 * and a reader must never have to guess which one they are looking at.
 */
---

<abbr
  class="est"
  title="This page counts bytes exactly and estimates tokens from them at 3.3 to 3.8 bytes per token. No tokenizer runs in your browser, so every token figure here is a band, not a count. Billed token counts come from the Anthropic API in CI, not from this page."
>est</abbr>
